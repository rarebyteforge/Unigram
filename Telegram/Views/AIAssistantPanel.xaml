<UserControl
    x:Class="Telegram.Views.AIAssistantPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource NavigationViewItemBackground}"
    BorderThickness="1,0,0,0"
    BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">

    <Grid ColumnSpacing="12" Padding="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Quick AI Actions for Chat -->
        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
            <TextBlock Text="AI Actions:" VerticalAlignment="Center" FontWeight="SemiBold"/>
        </StackPanel>

        <Button Grid.Column="1" Content="Summarize" Click="OnSummarizeClick" Style="{StaticResource AccentButtonStyle}"/>
        <Button Grid.Column="2" Content="Rewrite" Click="OnRewriteClick"/>
        <Button Grid.Column="3" Content="Translate" Click="OnTranslateClick"/>
    </Grid>
</UserControl>
